# Dependencias de tempo de execucao (espelho de pyproject.toml — fonte da verdade).
# Instalacao completa de desenvolvimento: pip install -e ".[dev]"
streamlit>=1.36
duckdb>=1.0
chromadb>=0.5
sentence-transformers>=3.0
rank-bm25>=0.9
openai>=1.30
python-dotenv>=1.0
pydantic>=2.7
pydantic-settings>=2.3
